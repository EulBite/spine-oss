{
  "name": "spine-sdk-node",
  "version": "0.1.0",
  "description": "Spine SDK for Node.js - Cryptographically signed audit logs",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/test/*.test.js",
    "example": "npm run build && node dist/examples/basic.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["audit", "logging", "cryptography", "compliance", "spine"],
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "blake3": "^2.1.7",
    "@noble/ed25519": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "files": ["dist", "README.md", "LICENSE"]
}
